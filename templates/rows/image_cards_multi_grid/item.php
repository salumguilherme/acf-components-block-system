<?php
	/**
	 * Flexible layout row item: Image Cards Multi Grid
	 *
	 * Stub. Override at {theme}/acbs/rows/image_cards_multi_grid/item.php
	 *
	 * Rendered once per item for a layout whose `source` field selects repeater, taxonomy
	 * or post type. Included with the item's own ACF loop active where the source is a
	 * repeater, so get_sub_field() reads the item's fields.
	 *
	 * @var ERDC\Modules\FlexibleLayoutTemplate\Rows\Row $row
	 *
	 * @version 1.0.0
	 * @since   1.0.0
	 */

	if ( ! defined( 'ABSPATH' ) ) { exit; }
